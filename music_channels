# Modify the search request to include the "q" parameter for the search query
request = youtube.search().list(
    part="snippet",
    eventType="live",
    maxResults=max_results,
    order="viewCount",
    type="video",
    q="Music",  # Add the q parameter for the search query
    pageToken=next_page_token
)
